# Reporte de Sesión — Historias 5.2 y 5.3: Recordatorios de Cobro y Agenda de Cobros

**Fecha:** 2026-09-21
**Historias:** 5.2 — Notificaciones Push Locales para Recordatorios de Cobro · 5.3 — Agenda y Calendario de Cobros (cierra Epic 5)
**Fase (ambas):** `bmad-create-story` → `bmad-dev-story` → code review → patches → `done`

---

## Resumen Ejecutivo

Dos historias completas en la sesión, con las tres de Epic 5 ya en `done`.

**5.2 — Recordatorios locales.** El proveedor recibe una notificación (cliente, monto, fecha) `dias_anticipacion_recordatorio` días antes de que venza una parcialidad; funciona offline, sin Firebase, y se reprograma sola cuando el valor cambia en Configuración. La decisión central fue un **reconciliador reactivo**: en vez de engancharse a los 5 puntos de escritura que afectan una parcialidad (alta, cobro, cancelación, pull, resolución de conflictos) más el cambio de Settings, un único observador de Room mantiene WorkManager sincronizado — cero cambios en el código existente.

**5.3 — Agenda de cobros (S-10).** Calendario personalizado de Compose con los cobros pendientes por día (fondo distinto + conteo superpuesto + campana dentro del umbral de alerta), descripciones de TalkBack por celda, lista del día y navegación a S-09. S-02 ganó su primer `TopAppBar` para alojar el ícono de calendario.

**Trampa evitada (5.2):** todos los workers de sync llevan `Constraints(NetworkType.CONNECTED)`; copiarlo al recordatorio habría roto el requisito offline en silencio.

**Estado final:** 5.2 y 5.3 → `done` ✅. **416 tests, 0 fallos** (358 al inicio de la sesión anterior → 416), `assembleDebug` exitoso. Epic 5: 3/3 historias.

---

## Historia 5.2 — Notificaciones Push Locales

- **Investigación:** solo `InstallmentEntity` tiene fecha de vencimiento (las ventas de pago único no), así que los recordatorios son por parcialidad; no existía infraestructura de notificaciones ni recursos `drawable`/`mipmap`. Durante la escritura detecté una inconsistencia en mi propio borrador: `replaceAll` debía recibir también las parcialidades abiertas, o se cancelaría un recordatorio ya encolado cuya hora pasó con el dispositivo apagado.
- **Implementación (T1-T16):** `ReminderPlanner` y `ReminderContentBuilder` puros; `WorkManagerReminderScheduler` (sin constraint de red); `ReminderWorker`; `ReminderReconciler` reactivo; canal, permiso `POST_NOTIFICATIONS` y solicitud en `MainScreen`. Bug de tests: `advanceUntilIdle()` no ejecuta corrutinas de `backgroundScope` (se usó `runCurrent()`).
- **Revisión (`/code-review`, 7 hallazgos → 5 patches, 1 diferido, 1 descartado):** el prompt de permiso se repetía en cada arranque en frío (ahora una vez por instalación), re-encolado de todo en cada emisión de Room (`distinctUntilChanged`), sin manejo de excepciones en el scope de aplicación (`.catch`), zona horaria evaluada una sola vez, `hashCode()` colisionable como ID de notificación. Descartado: el camino `installmentId == null` de `registerPayment` no es alcanzable porque `RegisterPaymentUseCase` lo bloquea.

## Historia 5.3 — Agenda y Calendario de Cobros

- **Investigación:** S-02 no tenía app bar; no existía ningún `Clock` en el proyecto (se agregó `TimeModule`); a 360dp con 16dp de padding las celdas medirían ≈46.9dp (<48dp de UX-DR20), de ahí el padding de 8dp; "Vencida" debe usar la misma regla que S-09 (`dueDate.isBefore(now)`).
- **Decisiones de alcance documentadas:** botones de mes anterior/siguiente (sin ellos las parcialidades futuras serían invisibles); el `OfflineBanner` global de `MainScreen` no se toca ("sin banner adicional" = S-10 no agrega uno propio); solo parcialidades `pending`.
- **Implementación (T1-T12):** `InstallmentDao.observeAgendaForTenant` (join con ventas y clientes), `AgendaCalculator` puro, `AgendaViewModel` con `Clock` inyectado, `AgendaScreen`, ruta y app bar de S-02.
- **Revisión (`bmad-code-review`, 3 capas: 13 + 11 + 9 hallazgos, deduplicados → 2 decisiones, 7 patches, 5 diferidos, 6 descartados).** El Acceptance Auditor no encontró violaciones bloqueantes. Decisiones tuyas: (1) **se mantiene** el sufijo ", con cobros próximos a vencer" en la descripción de TalkBack cuando hay campana — desviación aceptada y registrada del texto literal de AC-3; (2) **aviso de vencidos**: S-10 muestra "Tienes N cobros vencidos en meses anteriores" (los vencidos, los más urgentes, viven fuera del mes actual) y al tocarlo salta al mes del más antiguo. Patches: "hoy"/Vencida/campana se recalculan en cada `ON_RESUME` (y si cambió el día vuelve al mes actual; `Clock` con zona en vivo), la campana ya no incluye cobros de hoy que ya vencieron, la selección se limpia si su día se queda sin cobros o cambia de mes, rol/clic/`selected` declarados dentro de `clearAndSetSemantics`, y ajustes menores de T9.

---

## Verificación de build (final)

```bash
cd android && JAVA_HOME=".../Android Studio.app/Contents/jbr/Contents/Home" ./gradlew testDebugUnitTest assembleDebug
```

**Resultado:** 416 tests, 0 fallos, 0 errores. `assembleDebug` exitoso (valida el grafo de Hilt). Sin cambios de backend ni de schema Room en ninguna de las dos historias.

## Archivos

**5.2 — nuevos:** `sync/reminders/{ReminderPlanner,ReminderContentBuilder,ReminderScheduler,ReminderReconciler,NotificationChannels}.kt`, `sync/workers/ReminderWorker.kt`, 3 tests.
**5.3 — nuevos:** `domain/models/AgendaEntry.kt`, `di/TimeModule.kt`, `ui/screens/agenda/{AgendaCalculator,AgendaViewModel,AgendaScreen}.kt`, 2 tests.
**Modificados (entre ambas):** `InstallmentDao`, `SettingsRepository`, `SaleRepository`, `SyncModule`, `SumitrackApp`, `AndroidManifest.xml`, `MainScreen`, `NavGraph`, `Routes`, `OrderListScreen`, `OrderDetailScreen` (una visibilidad), `FakeInstallmentDao`, `SettingsRepositoryTest`, `SaleRepositoryTest`, `sprint-status.yaml`, `deferred-work.md`.

## Commits

- 5.2: `feat(5.2): Notificaciones Push Locales para Recordatorios de Cobro` — ya en `origin/main`.
- 5.3: `feat(5.3): Agenda y Calendario de Cobros` — commit de cierre de esta sesión (ver `git log`).

## Estado del Sprint

| Historia | Estado |
|----------|--------|
| Épica 1-4 | ✅ done |
| **Épica 5** | ✅ **3/3 historias done** (el estado del épico sigue `in-progress`: la transición es manual, igual que en Epic 4) |
| — 5.1 Configuración del Tenant | ✅ done |
| — 5.2 Notificaciones Push Locales | ✅ done |
| — 5.3 Agenda y Calendario de Cobros | ✅ done |

## Próximos pasos

- **Retrospectivas opcionales de Epic 4 y Epic 5** (`epic-4-retrospective`, `epic-5-retrospective`) y cierre manual del estado de ambos épicos; tras eso, el backlog original de historias está agotado — habría que decidir qué sigue (PRD/epics ya cubiertos).
- **Verificación manual en dispositivo (sin `adb` en este entorno) — acumulada de 5.2 y 5.3:** disparo real de WorkManager a la hora programada, prompt de `POST_NOTIFICATIONS` en Android 13+, tap en la notificación; y en S-10: apariencia de la cuadrícula, tamaños táctiles a 360dp, anuncio de TalkBack por celda, y que el `TopAppBar` nuevo de S-02 no duplique el inset de la barra de estado.
- **Deferred acumulado de las dos historias** (`deferred-work.md`): ícono propio para la notificación (usa uno del sistema), recuperación de recordatorios omitidos con notificaciones apagadas, error de Room mostrado como empty state, solapamiento con escala de fuente grande en las celdas, y un formateador de montos compartido con separador de miles (3 copias privadas hoy).
