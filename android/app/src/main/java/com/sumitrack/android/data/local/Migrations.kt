package com.sumitrack.android.data.local

import androidx.room.migration.Migration

object Migrations {
    // Add migrations here when the schema version increments.
    // Example: val MIGRATION_1_2 = Migration(1, 2) { db -> db.execSQL("ALTER TABLE ...") }
    val ALL: Array<Migration> = emptyArray()
}
